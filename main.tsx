import React from 'react'
import ReactDOM from 'react-dom/client'
import TwitterBlueprint from './App.tsx' // Make sure this matches your file name!

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TwitterBlueprint />
  </React.StrictMode>,
)
